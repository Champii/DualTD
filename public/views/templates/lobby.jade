div
  | Lobby
  button(ng-click="userService.logout()") Logout
  button(ng-click="randomGame()" ng-disabled="waiting") RandomGame
  div
    | {{message}}
  div(ng-repeat="user in lobbyService.list")
    b | Id:
    | {{user.id}}
    b | Login:
    | {{user.login}}

